CREATE TABLE PASSENGER_JOURNEY 
   (	PSG_JOURNEY_ID CHAR(6) NOT NULL ENABLE, 
	PSG_DATE DATE NOT NULL ENABLE, 
	PSG_DEP_TIME CHAR(8) NOT NULL ENABLE, 
	PSG_ARV_TIME CHAR(8) NOT NULL ENABLE, 
	PSG_JNEY_DURATION CHAR(8) NOT NULL ENABLE, 
	PSG_BUS_ID CHAR(6) NOT NULL ENABLE, 
	PSG_ROUTEJNEY_ID CHAR(6) NOT NULL ENABLE, 
	CONSTRAINT PSG_JOURNEY_ID_PK PRIMARY KEY (PSG_JOURNEY_ID)
  USING INDEX  ENABLE
   );

CREATE TABLE BUS_TAKEN
   (	PSG_BUS_ID CHAR(6) NOT NULL ENABLE, 
	PSG_JOURNEY_ID CHAR(6) NOT NULL ENABLE, 
	PSG_NO_PLATE CHAR(7) NOT NULL ENABLE, 
	PSG_DRIVER_ID CHAR(6) NOT NULL ENABLE, 
	CONSTRAINT BUS_TAKEN_PK PRIMARY KEY (PSG_BUS_ID, PSG_JOURNEY_ID)
  USING INDEX  ENABLE
   );

CREATE TABLE  PASSENGER_ROUTE 
   (	PSG_JOURNEY_ID CHAR(6) NOT NULL ENABLE, 
	PSG_RCODE CHAR(5) NOT NULL ENABLE, 
	PSG_DEP_PLACE VARCHAR2(20) NOT NULL ENABLE, 
	PSG_DESTINATION VARCHAR2(20) NOT NULL ENABLE, 
	PSG_BUS_FARE NUMBER(4,2) NOT NULL ENABLE, 
	CONSTRAINT PSG_ROUTE_PK PRIMARY KEY (PSG_RCODE, PSG_JOURNEY_ID)
  USING INDEX  ENABLE
   );

CREATE TABLE  BUSTAKEN_ROUTE 
   (	PSG_BUS_ID CHAR(6) NOT NULL ENABLE, 
	PSG_RCODE CHAR(5) NOT NULL ENABLE, 
	PSG_BUSROUTE_DESC VARCHAR2(30), 
	CONSTRAINT BUSTAKEN_ROUTE_PK PRIMARY KEY (PSG_BUS_ID, PSG_RCODE)
  USING INDEX  ENABLE
   );

CREATE TABLE  ROUTE_JOURNEY 
   (	PSG_ROUTEJNEY_ID CHAR(6) NOT NULL ENABLE, 
	PSG_RCODE CHAR(5) NOT NULL ENABLE, 
	CONSTRAINT PSG_ROUTEJNEY_ID_PK PRIMARY KEY (PSG_ROUTEJNEY_ID)
  USING INDEX  ENABLE
   );